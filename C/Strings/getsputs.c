#include <stdio.h>
#include <string.h>
int main(int argc, char const *argv[])
{
    char str[15];
    puts("Enter String ");
    gets(str);
    printf("The revese is : ");
    puts(str);
    return 0;
}