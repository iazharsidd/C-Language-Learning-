#include <stdio.h>
int main(int argc, char const *argv[])
{
    int arr[10]={1,2,3,4,4,6,7,8,9};
    int brr[10];
    for (int i = 0; i < 9; i++)
    {
      int j = arr[i];
        brr[j]=1;
      // for (int j = i+1; j < 9; j++)
      // {
      //   if (arr[i]==arr[j])
      //   {
      //       printf("%d,%d ",arr[i],arr[j]);
      //   }
        if()
    }
    return 0;
}
